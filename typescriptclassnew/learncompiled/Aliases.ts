console.log('WELCOME TO ALIASES');

//aliases are nickname for creating template of data types 
// we use the keyword "type" to declare a type. 

type aparam= (string| number|(number|object)[] | object | boolean);
type bparam= (Boolean[] | "Yes" | "No" | object);



// function signature in typescript 
